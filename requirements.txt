# 跨境电商ERP自动化 — Python 依赖
# 安装: pip install -r requirements.txt

# 浏览器自动化（CDP 连接）
playwright>=1.40.0

# HTTP 请求（llm_client.py）
requests>=2.28.0

# 配置管理
pyyaml>=6.0
python-dotenv>=1.0.0

# LLM 调用（llm_client.py 底层用 openai SDK）
openai>=1.0.0

# LangChain（image_checker/taiwan_regulation 用）
langchain-openai>=0.2.0
langchain-core>=0.3.0
